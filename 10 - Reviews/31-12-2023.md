---
type: journal
category: daily-note 
---

------------------------------------------------------------------------
## Direction:

Forcing everything I learnt to 2024 to create the best year ever


------------------------------------------------------------------------

## Administration
- [x] Mail
- [x] Calendar
- [x] Asana

-----------------------------------------------------------------

## Clinical work
	channels : 
	oDoc : 0
	audio : 0
	surgeries : 
	postop : 
	Inward patients: 
	Referrals: 
	Endoscopies: 

------------------------------------------------------------------------



# What need to be achieved today:

To do:
- [ ] 
- [ ] 
------------------------------------------------------------------------


Thoughts:

```dataview
LIST
WHERE type = "journal"
WHERE contains(category,"daily-note")
```












```dataview 
TABLE
file.name as title, file.cname as date
where type = "journal"
sort file.ctime desc
```

```dataviewjs
for(
	let p of dv.pages('"journal"')
	.sort(page => page.file.name, "desc")
	.where(page => 
		(page.file.name >= "2022-07-01") &&
		(page.file.name <= "2022-07-31"))
) 
{
	dv.header(3, p.file.name);
	dv.paragraph("![[" + p.file.name + "]]")
}
```


```dataviewjs
for(
	let p of dv.pages('"Reviews"')
	.sort(page => page.file.name, "desc")
	.where(page => 
		(page.file.name >= "2022-07-01") &&
		(page.file.name <= "2022-07-31"))
) 
{
	dv.header(3, p.file.name);
	dv.paragraph("![[" + p.file.name + "]]")
}
```
